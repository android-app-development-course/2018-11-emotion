<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hero.emotion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Main2Activity"
            android:theme="@style/AppTheme.NoBar" />
        <activity android:name=".aboutActivity" android:theme="@style/AppTheme.NoBar"/>
        <activity android:name=".SecondActivity" android:theme="@style/AppTheme.NoBar"/>
        <activity android:name=".ThirdActivity" android:theme="@style/AppTheme.NoBar"/>
        <activity android:name=".what" android:theme="@style/AppTheme.NoBar"/>
        <activity android:name=".treat" android:theme="@style/AppTheme.NoBar"/>



    </application>

</manifest>